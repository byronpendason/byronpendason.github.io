---
layout: null
sitemap:
  exclude: 'yes'
---

[
  {% for post in site.pages %}{% unless post.sitemap.exclude == 'yes' or page.name == 'feed.xml' or page.name == "style.css" %}
    {
      "title"    : "{{ post.title }}",
      "url"      : "{{ site.url }}{{ post.url }}",
      "date"     : "{{ post.date | date_to_string }}",
      "description" : "{{ post.excerpt | strip_html | strip_newlines | escape }}"
    },
  {% endunless %}{% endfor %}
  {% for post in site.posts %}
    {
      "title"    : "{{ post.title }}",
      "url"      : "{{ site.url }}{{ post.url }}",
      "date"     : "{{ post.date | date_to_string }}",
      "description" : "{{ post.excerpt | strip_html | strip_newlines | escape }}"
    } {% unless forloop.last %},{% endunless %}
  {% endfor %}
]
