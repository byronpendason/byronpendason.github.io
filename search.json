---
layout: nil
sitemap:
  exclude: 'yes'
---

[
  {% for post in site.pages %}{% unless post.sitemap.exclude == 'yes' or post.url == '/feed.xml' or post.url == '/assets/css/style.css' %}
    {
      "title"    : "{{ post.title }}",
      "url"      : "{{ site.url }}{{ post.url }}",
      "date"     : "{{ post.date | date_to_string }}",
      "description" : "{{ post.content | strip_html | strip_newlines | escape | truncate:100 }}"
    },
  {% endunless %}{% endfor %}
  {% for post in site.posts %}
    {
      "title"    : "{{ post.title }}",
      "url"      : "{{ site.url }}{{ post.url }}",
      "date"     : "{{ post.date | date_to_string }}",
      "description" : "{{ post.excerpt | strip_html | strip_newlines | escape | truncate:100 }}"
    } {% unless forloop.last %},{% endunless %}
  {% endfor %}
]
